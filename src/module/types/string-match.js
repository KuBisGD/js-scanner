/**
 * Module for a string matching type.
 * 
 * @author Edwin Johnsson ej224wy@student.lnu.se
 * @version 1.0.0
 */

/**
 * Object that defines a numeric range.
 * 
 * @typedef {object} StringMatch
 * @property {RegExp} pattern Regex pattern to match a string to.
 * @property {number} min Minimum value.
 * @property {number} max Maximum value.
 */

export default StringMatch
