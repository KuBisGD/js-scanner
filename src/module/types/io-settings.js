/**
 * Module for IO settings type.
 * 
 * @author Edwin Johnsson ej224wy@student.lnu.se
 * @version 1.0.0
 */

/**
 * Settings for IO.
 * 
 * @typedef {object} IOSettings
 * @property {Node.ReadableStream} [input=process.stdin] Input stream.
 * @property {Node.WritableStream} [output=process.stdout] Output stream.
 */

export default IOSettings
